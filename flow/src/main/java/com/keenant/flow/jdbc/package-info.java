/**
 * JDBC uses some "magic values" which are a relic from before Java 5 (Enums were introduced).
 *
 * Here we have some enums that represent these magic values, which are used to configure
 * an SQL query.
 */
package com.keenant.flow.jdbc;